
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BetVital Admin | Manual Entry</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap');
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-950 text-slate-100 min-h-screen">
    <div class="max-w-7xl mx-auto py-16 px-6">
        <div class="flex justify-between items-end mb-16">
            <div>
                <h1 class="text-4xl font-black tracking-tighter text-white">STAFF DASHBOARD</h1>
                <p class="text-slate-500 font-bold uppercase text-[10px] tracking-[0.4em] mt-2">Manual Prediction Management</p>
            </div>
            <div class="flex gap-4">
                <a href="/setup" class="px-6 py-3 bg-slate-800 hover:bg-slate-700 rounded-xl text-[10px] font-black transition-all uppercase tracking-widest border border-slate-700">Reset DB/Setup</a>
                <a href="/" class="px-6 py-3 bg-emerald-600 hover:bg-emerald-500 rounded-xl text-[10px] font-black transition-all uppercase tracking-widest text-white shadow-lg shadow-emerald-900/40">View Live Site</a>
            </div>
        </div>

        <div class="grid lg:grid-cols-12 gap-10">
            <div class="lg:col-span-4">
                <div class="bg-slate-900 rounded-[2.5rem] p-10 border border-slate-800 shadow-2xl sticky top-24">
                    <h2 class="text-xl font-black mb-8 text-emerald-400 uppercase tracking-tight flex items-center gap-3">
                        <i class="fa-solid fa-plus-circle"></i> New Entry
                    </h2>
                    <form action="/admin/add" method="POST" class="space-y-6">
                        <div class="space-y-2">
                            <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest">League</label>
                            <select name="league_id" required class="w-full bg-slate-800 border border-slate-700 p-4 rounded-2xl text-sm outline-none focus:ring-2 focus:ring-emerald-500 transition-all text-white">
                                <% leagues.forEach(l => { %>
                                    <option value="<%= l.id %>"><%= l.name %></option>
                                <% }) %>
                            </select>
                        </div>
                        <div class="grid grid-cols-2 gap-4">
                            <div class="space-y-2">
                                <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest">Home Team</label>
                                <select name="home_team_id" required class="w-full bg-slate-800 border border-slate-700 p-4 rounded-2xl text-sm outline-none focus:ring-2 focus:ring-emerald-500 text-white">
                                    <% teams.forEach(t => { %>
                                        <option value="<%= t.id %>"><%= t.name %></option>
                                    <% }) %>
                                </select>
                            </div>
                            <div class="space-y-2">
                                <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest">Away Team</label>
                                <select name="away_team_id" required class="w-full bg-slate-800 border border-slate-700 p-4 rounded-2xl text-sm outline-none focus:ring-2 focus:ring-emerald-500 text-white">
                                    <% teams.forEach(t => { %>
                                        <option value="<%= t.id %>"><%= t.name %></option>
                                    <% }) %>
                                </select>
                            </div>
                        </div>

                        <div class="grid grid-cols-2 gap-3">
                            <input type="text" name="home_form" placeholder="Home Form (W,D,L)" class="bg-slate-800 text-[11px] font-bold p-4 rounded-xl border border-slate-700 outline-none focus:border-emerald-500">
                            <input type="text" name="away_form" placeholder="Away Form (W,D,L)" class="bg-slate-800 text-[11px] font-bold p-4 rounded-xl border border-slate-700 outline-none focus:border-emerald-500">
                        </div>

                        <div class="space-y-2">
                            <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest">Analysis Content</label>
                            <textarea name="analysis_content" required class="w-full bg-slate-800 border border-slate-700 p-4 rounded-2xl h-32 text-sm outline-none focus:ring-2 focus:ring-emerald-500 text-slate-300" placeholder="Write your expert analysis here..."></textarea>
                        </div>

                        <div class="grid grid-cols-2 gap-4">
                            <input type="text" name="prediction_main" required placeholder="Main Tip (e.g. Over 2.5)" class="bg-slate-800 border border-slate-700 p-4 rounded-2xl text-sm outline-none focus:ring-2 focus:ring-emerald-500 text-white">
                            <input type="number" name="prediction_confidence" required placeholder="Conf. %" class="bg-slate-800 border border-slate-700 p-4 rounded-2xl text-sm outline-none focus:ring-2 focus:ring-emerald-500 text-white">
                        </div>

                        <div class="space-y-2">
                            <label class="text-[9px] font-black text-slate-500 uppercase tracking-widest">Match Date & Time</label>
                            <input type="datetime-local" name="match_date" required class="w-full bg-slate-800 border border-slate-700 p-4 rounded-2xl text-sm outline-none focus:ring-2 focus:ring-emerald-500 text-white">
                        </div>

                        <button type="submit" class="w-full py-5 bg-emerald-600 hover:bg-emerald-500 text-white rounded-[1.5rem] font-black uppercase tracking-[0.2em] text-xs transition-all shadow-xl shadow-emerald-900/20">
                            Publish To Feed
                        </button>
                    </form>
                </div>
            </div>

            <div class="lg:col-span-8">
                <div class="bg-slate-900 rounded-[2.5rem] border border-slate-800 overflow-hidden shadow-2xl">
                    <div class="p-8 border-b border-slate-800 flex justify-between items-center">
                        <h3 class="font-black text-sm uppercase tracking-widest">Recent Publications</h3>
                        <span class="text-[10px] font-bold text-slate-500 uppercase tracking-widest">Total Matches: <%= matches.length %></span>
                    </div>
                    <div class="overflow-x-auto">
                        <table class="w-full text-left text-sm">
                            <thead class="bg-slate-800/30 text-[10px] font-black uppercase text-slate-500 tracking-[0.2em]">
                                <tr>
                                    <th class="px-8 py-6">Fixture</th>
                                    <th class="px-8 py-6">Tip</th>
                                    <th class="px-8 py-6 text-center">Confidence</th>
                                    <th class="px-8 py-6 text-right">Status</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-slate-800">
                                <% matches.forEach(match => { %>
                                    <tr class="hover:bg-slate-800/30 transition-colors group">
                                        <td class="px-8 py-6">
                                            <div class="flex flex-col">
                                                <span class="font-black text-slate-200 uppercase tracking-tight">
                                                    <%= match.HomeTeam ? match.HomeTeam.name : '???' %> vs <%= match.AwayTeam ? match.AwayTeam.name : '???' %>
                                                </span>
                                                <span class="text-[9px] font-bold text-slate-600 uppercase tracking-widest mt-1"><%= match.League ? match.League.name : 'General' %></span>
                                            </div>
                                        </td>
                                        <td class="px-8 py-6">
                                            <span class="px-3 py-1 bg-emerald-950 text-emerald-400 rounded-lg text-[10px] font-black uppercase border border-emerald-900"><%= match.prediction_main %></span>
                                        </td>
                                        <td class="px-8 py-6 text-center font-black text-slate-400 text-lg">
                                            <%= match.prediction_confidence %>%
                                        </td>
                                        <td class="px-8 py-6 text-right">
                                            <span class="px-2 py-0.5 bg-slate-800 text-slate-500 rounded text-[9px] font-black uppercase tracking-widest border border-slate-700"><%= match.status %></span>
                                        </td>
                                    </tr>
                                <% }) %>
                                <% if (matches.length === 0) { %>
                                    <tr>
                                        <td colspan="4" class="px-8 py-20 text-center">
                                            <p class="text-slate-600 font-black uppercase tracking-[0.4em] italic text-xs">No records available.</p>
                                        </td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
