<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= match.home_team %> vs <%= match.away_team %> Prediction</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <nav class="bg-emerald-700 text-white p-4">
        <div class="container mx-auto">
            <a href="/" class="font-bold text-xl">BetVital</a>
        </div>
    </nav>

    <div class="container mx-auto p-4 max-w-2xl">
        <div class="bg-white rounded-lg shadow-lg p-6">
            <div class="text-center mb-6">
                <div class="text-sm text-gray-500 uppercase"><%= match.league %></div>
                <h1 class="text-3xl font-bold my-2"><%= match.home_team %> <span class="text-gray-400">vs</span> <%= match.away_team %></h1>
                <div class="text-gray-600"><%= new Date(match.match_date).toLocaleString() %></div>
            </div>

            <div class="bg-gray-50 p-4 rounded border mb-6 text-center">
                <div class="text-sm text-gray-500">Our Prediction</div>
                <div class="text-2xl font-bold text-emerald-600"><%= match.prediction %></div>
                <div class="text-xs text-gray-400 mt-1">Confidence: <%= match.confidence %>%</div>
            </div>

            <div class="prose max-w-none">
                <h3 class="font-bold text-lg">Analysis</h3>
                <p class="whitespace-pre-wrap"><%= match.analysis %></p>
            </div>
            
            <% if(match.affiliate_link) { %>
            <div class="mt-8">
                <a href="<%= match.affiliate_link %>" target="_blank" class="block w-full bg-yellow-500 hover:bg-yellow-600 text-black font-bold text-center py-4 rounded animation-pulse">
                    Bet on this Match Now
                </a>
            </div>
            <% } %>
        </div>
    </div>
</body>
</html>
