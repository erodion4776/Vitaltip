<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BetVital | Professional Football Tips</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-50 text-gray-900">

    <!-- Top Status Bar -->
    <div class="bg-slate-900 text-slate-300 text-[10px] py-2 px-4 flex justify-between">
        <div class="flex items-center gap-2">
            <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></span>
            <span>Live Analysis Engine: Online</span>
        </div>
        <div class="flex gap-4">
            <span class="text-yellow-500 font-bold">18+ Gamble Responsibly</span>
            <a href="/admin/login" class="hover:text-white">Staff Login</a>
        </div>
    </div>

    <!-- Main Navigation -->
    <nav class="bg-white shadow-sm sticky top-0 z-50 border-b border-gray-200">
        <div class="container mx-auto px-4 h-16 flex justify-between items-center">
            <a href="/" class="flex items-center gap-2">
                <div class="w-8 h-8 bg-emerald-700 rounded-lg flex items-center justify-center text-white font-black text-lg shadow-lg">B</div>
                <span class="text-xl font-bold tracking-tight text-slate-800">BetVital</span>
            </a>
            
            <div class="flex gap-2">
                <a href="/" class="px-4 py-2 text-xs font-bold bg-emerald-50 text-emerald-700 rounded-full border border-emerald-200">Predictions</a>
                <a href="/results" class="px-4 py-2 text-xs font-bold text-gray-500 hover:text-gray-900 transition">Results</a>
            </div>
        </div>
    </nav>

    <!-- Date Filter -->
    <div class="bg-white border-b border-gray-200 overflow-x-auto">
        <div class="container mx-auto flex justify-start sm:justify-center px-4">
            <a href="/results" class="px-6 py-4 text-xs font-bold text-gray-400 hover:text-gray-800 border-b-2 border-transparent hover:border-gray-300 whitespace-nowrap">Yesterday</a>
            <div class="px-6 py-4 text-xs font-bold text-emerald-700 border-b-2 border-emerald-600 bg-emerald-50/50 whitespace-nowrap">Today's Tips</div>
            <div class="px-6 py-4 text-xs font-bold text-gray-300 cursor-not-allowed whitespace-nowrap">Tomorrow</div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container mx-auto p-3 sm:p-6 max-w-4xl min-h-screen">
        
        <div class="mb-6 flex justify-between items-end">
            <div>
                <h1 class="text-lg font-bold text-slate-800">Featured Matches</h1>
                <p class="text-xs text-gray-500">Mathematical predictions sorted by confidence.</p>
            </div>
            <span class="text-[10px] bg-red-100 text-red-600 px-2 py-1 rounded font-bold uppercase tracking-wider">Updates: Live</span>
        </div>

        <div class="space-y-3">
            <% if(matches.length === 0) { %>
                <div class="bg-white p-12 text-center rounded-xl shadow-sm border border-gray-200">
                    <div class="text-gray-400 text-4xl mb-3">⚽</div>
                    <h3 class="text-gray-900 font-bold">No tips available right now</h3>
                    <p class="text-gray-500 text-xs mt-1">Our analysts are processing the data. Check back shortly.</p>
                </div>
            <% } %>

            <% matches.forEach(match => { %>
            <a href="/prediction/<%= match.slug %>" class="block bg-white rounded-xl shadow-sm border border-gray-100 hover:shadow-md hover:border-emerald-200 transition-all duration-200 group relative overflow-hidden">
                
                <!-- Confidence Stripe -->
                <div class="absolute left-0 top-0 bottom-0 w-1 <%= match.confidence > 80 ? 'bg-emerald-500' : (match.confidence > 60 ? 'bg-yellow-500' : 'bg-gray-300') %>"></div>

                <div class="p-4 flex items-center justify-between">
                    
                    <!-- Left: Time & League -->
                    <div class="w-16 sm:w-24 text-center">
                        <div class="text-xs font-bold text-gray-800 font-mono">
                            <%= new Date(match.match_date).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit', hour12: false}) %>
                        </div>
                        <div class="text-[9px] text-gray-400 uppercase font-bold tracking-wider mt-0.5 truncate"><%= match.league %></div>
                    </div>

                    <!-- Middle: Teams -->
                    <div class="flex-1 px-2 sm:px-6 grid grid-cols-1 sm:grid-cols-3 items-center gap-2">
                        <!-- Home -->
                        <div class="flex items-center gap-3 sm:justify-end">
                            <span class="text-sm font-bold text-slate-800 truncate"><%= match.home_team %></span>
                            <% if(match.home_logo) { %> <img src="<%= match.home_logo %>" class="w-6 h-6 object-contain"> <% } else { %> <div class="w-6 h-6 bg-gray-200 rounded-full"></div> <% } %>
                        </div>
                        
                        <!-- VS -->
                        <div class="hidden sm:flex justify-center">
                            <span class="text-[10px] font-bold text-gray-300 bg-gray-100 px-2 py-0.5 rounded">VS</span>
                        </div>

                        <!-- Away -->
                        <div class="flex items-center gap-3">
                            <% if(match.away_logo) { %> <img src="<%= match.away_logo %>" class="w-6 h-6 object-contain"> <% } else { %> <div class="w-6 h-6 bg-gray-200 rounded-full"></div> <% } %>
                            <span class="text-sm font-bold text-slate-800 truncate"><%= match.away_team %></span>
                        </div>
                    </div>

                    <!-- Right: Prediction -->
                    <div class="w-24 sm:w-32 text-right">
                        <div class="inline-flex flex-col items-end">
                            <span class="bg-slate-900 text-white text-xs font-bold px-2.5 py-1 rounded shadow-sm group-hover:bg-emerald-600 transition">
                                <%= match.prediction %>
                            </span>
                            <div class="flex items-center gap-1 mt-1.5">
                                <span class="text-[9px] text-gray-400 font-semibold"><%= match.confidence %>% Conf.</span>
                                <div class="w-8 h-1 bg-gray-100 rounded-full overflow-hidden">
                                    <div class="h-full bg-emerald-500" style="width: <%= match.confidence %>%"></div>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </a>
            <% }) %>
        </div>
        
        <!-- Trust Footer -->
        <div class="mt-12 border-t border-gray-200 pt-8 pb-4 text-center">
            <h4 class="text-xs font-bold text-gray-400 uppercase tracking-widest mb-2">BetVital Professional Tips</h4>
            <p class="text-[10px] text-gray-400 max-w-md mx-auto leading-relaxed">
                Gambling involves risk. Please gamble responsibly. Our predictions are based on statistics and do not guarantee success. 18+ Only.
            </p>
        </div>
    </div>
</body>
</html>
