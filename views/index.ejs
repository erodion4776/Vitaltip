
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BetVital | <%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800;900&display=swap');
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-950 text-slate-100">
    <!-- Navbar -->
    <nav class="border-b border-slate-800 bg-slate-950/80 backdrop-blur-md sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 h-20 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <div class="bg-emerald-600 text-white w-10 h-10 flex items-center justify-center rounded-xl font-black text-xl shadow-lg shadow-emerald-600/20">BV</div>
                <h1 class="text-2xl font-black tracking-tighter uppercase italic">Bet<span class="text-emerald-500">Vital</span></h1>
            </div>
            <div class="flex items-center gap-8">
                <a href="/" class="text-sm font-bold text-emerald-500 uppercase tracking-widest">Feed</a>
                <a href="/admin/add" class="text-sm font-bold text-slate-400 hover:text-white uppercase tracking-widest transition-colors">Add Tip</a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-6xl mx-auto px-6 py-16">
        <header class="mb-16 text-center">
            <h2 class="text-5xl md:text-7xl font-black tracking-tight mb-4 uppercase italic">Elite Match <span class="text-emerald-500 underline decoration-slate-800 underline-offset-8">Analysis.</span></h2>
            <p class="text-slate-500 text-lg max-w-2xl mx-auto font-medium">Get the winning edge with professional tactical breakdowns and manual expert predictions.</p>
        </header>

        <div class="grid gap-12">
            <% if (!matches || matches.length === 0) { %>
                <div class="py-24 text-center bg-slate-900/50 rounded-[3rem] border-2 border-dashed border-slate-800">
                    <div class="bg-slate-800 w-20 h-20 flex items-center justify-center rounded-full mx-auto mb-6">
                        <i class="fa-solid fa-futbol text-3xl text-slate-600"></i>
                    </div>
                    <h3 class="text-xl font-black text-slate-300 mb-2">NO PREDICTIONS YET</h3>
                    <p class="text-slate-500 mb-8 max-w-xs mx-auto">The stadium is empty. Be the first to add an expert prediction.</p>
                    <a href="/admin/add" class="px-8 py-4 bg-emerald-600 hover:bg-emerald-500 text-white rounded-2xl font-black uppercase tracking-widest text-xs transition-all">Add First Match</a>
                </div>
            <% } else { %>
                <div class="grid md:grid-cols-2 gap-8">
                    <% matches.forEach(match => { %>
                        <div class="bg-slate-900 border border-slate-800 rounded-[2.5rem] overflow-hidden hover:border-emerald-500/50 transition-all duration-500 group shadow-2xl">
                            <div class="p-8 border-b border-slate-800 flex justify-between items-center bg-slate-900/50">
                                <span class="text-[10px] font-black uppercase tracking-[0.3em] text-emerald-500"><%= match.League ? match.League.name : 'Unknown League' %></span>
                                <span class="text-[10px] font-bold text-slate-500 uppercase tracking-widest"><%= new Date(match.match_date).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', hour: '2-digit', minute: '2-digit' }) %></span>
                            </div>
                            
                            <div class="p-10">
                                <div class="flex items-center justify-between mb-10">
                                    <div class="flex-1 text-center">
                                        <div class="w-16 h-16 bg-slate-800 rounded-2xl mx-auto mb-4 flex items-center justify-center font-black text-slate-400 group-hover:bg-emerald-600/10 group-hover:text-emerald-500 transition-all">
                                            <%= match.HomeTeam ? match.HomeTeam.name.substring(0,2).toUpperCase() : 'H' %>
                                        </div>
                                        <div class="font-black text-xs uppercase tracking-tight text-white"><%= match.HomeTeam ? match.HomeTeam.name : 'Home' %></div>
                                        <div class="text-[9px] font-black text-emerald-600 mt-2 tracking-widest opacity-60"><%= match.home_form %></div>
                                    </div>
                                    <div class="px-6 text-3xl font-black text-slate-800 italic group-hover:text-slate-700 transition-colors">VS</div>
                                    <div class="flex-1 text-center">
                                        <div class="w-16 h-16 bg-slate-800 rounded-2xl mx-auto mb-4 flex items-center justify-center font-black text-slate-400 group-hover:bg-emerald-600/10 group-hover:text-emerald-500 transition-all">
                                            <%= match.AwayTeam ? match.AwayTeam.name.substring(0,2).toUpperCase() : 'A' %>
                                        </div>
                                        <div class="font-black text-xs uppercase tracking-tight text-white"><%= match.AwayTeam ? match.AwayTeam.name : 'Away' %></div>
                                        <div class="text-[9px] font-black text-emerald-600 mt-2 tracking-widest opacity-60"><%= match.away_form %></div>
                                    </div>
                                </div>

                                <div class="bg-emerald-600 rounded-[2rem] p-6 flex items-center justify-between shadow-xl shadow-emerald-900/20 mb-8 border border-emerald-500">
                                    <div>
                                        <div class="text-[9px] font-black text-emerald-100 uppercase tracking-widest mb-1">PRO TIP</div>
                                        <div class="text-xl font-black text-white uppercase tracking-tight"><%= match.prediction_main %></div>
                                    </div>
                                    <div class="text-right">
                                        <div class="text-[9px] font-black text-emerald-100 uppercase tracking-widest mb-1">CONFIDENCE</div>
                                        <div class="text-2xl font-black text-white"><%= match.prediction_confidence %>%</div>
                                    </div>
                                </div>

                                <div class="text-sm text-slate-400 leading-relaxed italic line-clamp-3">
                                    "<%= match.analysis_content %>"
                                </div>

                                <div class="mt-8 flex gap-4">
                                    <button class="flex-1 py-4 bg-slate-800 hover:bg-slate-700 rounded-xl font-black text-[10px] uppercase tracking-widest transition-all">Full Analysis</button>
                                    <% if (match.affiliate_link) { %>
                                        <a href="<%= match.affiliate_link %>" target="_blank" class="px-6 py-4 bg-emerald-600/20 text-emerald-500 rounded-xl flex items-center justify-center hover:bg-emerald-600 hover:text-white transition-all">
                                            <i class="fa-solid fa-up-right-from-square"></i>
                                        </a>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="py-20 border-t border-slate-900 text-center">
        <p class="text-slate-600 text-[10px] font-black uppercase tracking-[0.5em]">&copy; 2024 BetVital SSR Engine</p>
    </footer>
</body>
</html>
