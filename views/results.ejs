<!DOCTYPE html>
<html lang="en">
<head>
    <title>Match Results - BetVital</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">

    <!-- Header -->
    <nav class="bg-emerald-700 text-white p-4 shadow-md sticky top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-2xl font-bold tracking-tighter">BetVital</a>
            <div class="space-x-2">
                <a href="/" class="text-sm text-emerald-100 hover:text-white">Upcoming</a>
                <a href="/results" class="text-sm bg-white text-emerald-800 px-3 py-1 rounded font-bold">Results</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-4 max-w-4xl">
        <h1 class="text-xl font-bold mb-4 text-gray-700 border-l-4 border-emerald-500 pl-3">Recent Results</h1>
        
        <div class="bg-white rounded-lg shadow overflow-hidden">
            <table class="w-full text-sm text-left">
                <thead class="bg-gray-800 text-white text-xs uppercase">
                    <tr>
                        <th class="p-3 w-20">Date</th>
                        <th class="p-3">Match</th>
                        <th class="p-3 hidden sm:table-cell">Tip</th>
                        <th class="p-3 text-center">Score</th>
                        <th class="p-3 text-center w-20">Status</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-100">
                    <% if(matches.length === 0) { %>
                        <tr><td colspan="5" class="p-8 text-center text-gray-500">No results posted yet. Check back later!</td></tr>
                    <% } %>

                    <% matches.forEach(match => { %>
                    <tr class="hover:bg-gray-50 transition">
                        <td class="p-3 text-gray-500 text-xs">
                            <%= new Date(match.match_date).toLocaleDateString(undefined, {month:'short', day:'numeric'}) %>
                            <div class="sm:hidden text-[10px] mt-1"><%= new Date(match.match_date).toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'}) %></div>
                        </td>
                        <td class="p-3">
                            <div class="font-bold text-gray-800"><%= match.home_team %> <span class="text-gray-400 font-normal">v</span> <%= match.away_team %></div>
                            <div class="text-[10px] text-gray-400 uppercase tracking-wide"><%= match.league %></div>
                            <div class="sm:hidden text-xs text-emerald-600 font-bold mt-1">Tip: <%= match.prediction %></div>
                        </td>
                        <td class="p-3 hidden sm:table-cell font-bold text-emerald-700"><%= match.prediction %></td>
                        <td class="p-3 text-center font-mono font-bold text-lg text-blue-900"><%= match.result_score %></td>
                        <td class="p-3 text-center">
                            <% if(match.bet_status === 'won') { %>
                                <span class="bg-green-500 text-white px-2 py-1 rounded text-[10px] font-bold uppercase tracking-wider">WON</span>
                            <% } else if(match.bet_status === 'lost') { %>
                                <span class="bg-red-500 text-white px-2 py-1 rounded text-[10px] font-bold uppercase tracking-wider">LOST</span>
                            <% } else { %>
                                <span class="bg-gray-400 text-white px-2 py-1 rounded text-[10px] font-bold uppercase tracking-wider">VOID</span>
                            <% } %>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Footer -->
    <footer class="mt-8 text-center text-gray-400 text-xs pb-4">
        &copy; <%= new Date().getFullYear() %> BetVital Prediction Platform
    </footer>

</body>
</html>
