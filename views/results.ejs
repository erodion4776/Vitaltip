<!DOCTYPE html>
<html lang="en">
<head>
    <title>Results - BetVital</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-gray-50 text-gray-900">

    <nav class="bg-emerald-700 text-white shadow-lg p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="font-bold text-xl tracking-tight">BetVital Results</a>
            <a href="/" class="text-xs bg-white text-emerald-800 font-bold px-3 py-1.5 rounded">Upcoming</a>
        </div>
    </nav>

    <div class="container mx-auto p-4 max-w-4xl">
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
            <div class="bg-gray-100 px-4 py-3 border-b border-gray-200">
                <h2 class="text-sm font-bold text-gray-600 uppercase">Recent Performance</h2>
            </div>
            
            <table class="w-full text-sm text-left">
                <tbody class="divide-y divide-gray-100">
                    <% if(matches.length === 0) { %>
                        <tr><td class="p-8 text-center text-gray-500">No finished games yet.</td></tr>
                    <% } %>

                    <% matches.forEach(match => { %>
                    <tr class="hover:bg-gray-50">
                        <td class="p-4">
                            <div class="text-[10px] text-gray-400 font-bold uppercase"><%= new Date(match.match_date).toLocaleDateString() %></div>
                            <div class="font-bold text-gray-800"><%= match.home_team %> <span class="text-gray-400 font-normal">v</span> <%= match.away_team %></div>
                            <div class="text-xs text-emerald-600 font-bold mt-1">Tip: <%= match.prediction %></div>
                        </td>
                        <td class="p-4 text-right">
                            <div class="text-xl font-mono font-bold text-slate-800 mb-1"><%= match.result_score %></div>
                            
                            <% if(match.bet_status === 'won') { %>
                                <span class="bg-green-100 text-green-700 text-[10px] font-black px-2 py-1 rounded uppercase tracking-wider">WON</span>
                            <% } else if(match.bet_status === 'lost') { %>
                                <span class="bg-red-100 text-red-700 text-[10px] font-black px-2 py-1 rounded uppercase tracking-wider">LOST</span>
                            <% } else { %>
                                <span class="bg-gray-200 text-gray-600 text-[10px] font-black px-2 py-1 rounded uppercase">VOID</span>
                            <% } %>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
